# Airline-Booking-System
  A simple system to book flights with preferences as desired. Added capability to view and cancel flights; with refunds being processed in line with the cancellation policy. Designed in python, with MySQL as database and Tkinter as GUI to simulate a simple real world application.
